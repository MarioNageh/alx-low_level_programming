#ifndef _MAIN_H
#define _MAIN_H

#include <stdio.h>
#include <unistd.h>
#include <string.h>

/* Strings */
int strlen_m(char *c);
int _putchar(char c);


/* Bit Manipulation */
unsigned int binary_to_uint(const char *c);

#endif
