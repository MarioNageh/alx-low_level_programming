#include "main.h"

/**
* Description: _strcat - Concatenate Two Strings
* @dest: pointer char * to Destination
* @src:  pointer char * to Source
* Return: char * to concatened string
*/

char *_strcat(char *dest, char *src)
{
return (strcat(dest, src));
}
